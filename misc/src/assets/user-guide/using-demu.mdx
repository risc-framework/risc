import { InfoBox, TipBox, SuccessBox } from '@components'

# DEMU Simulator

```info
This project is still under active development. 
```

`demu` is a **RTL-level** cpu and system simulator built with **Verilator**.

## Prerequisites

- **C/C++ Compiler(gcc/clang etc.)**
- **Make(Ninja)/CMake( >= 3.24)**
- **[spdlog](https://github.com/gabime/spdlog.git)**
- **[Verilator](https://github.com/verilator/verilator.git)**
- **lit(optional)**

## Building the Simulator

### Quick Build

```bash
make sim # Use `make help` to see make options.
```
